Create a file containing the AI prompt: "Create a python script to add today's date (YYYY-MM-DD) to the beginning of every filename in a folder."

```python
with open("prompt.txt", "w") as f:
    f.write("Create a python script to add today's date (YYYY-MM-DD) to the beginning of every filename in a folder.")
```
